import 'package:flutter/material.dart';
import '../gallery_logo.dart';

class tabBar_2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child:logo(),
    );
  }
}