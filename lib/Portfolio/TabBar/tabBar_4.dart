import 'package:flutter/material.dart';
import '../gallery_logo.dart';

class tabBar_4 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      child:logo(),
    );
  }
}